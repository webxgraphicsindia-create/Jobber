import 'package:flutter/material.dart';

class AppColors {
  // Adding your custom color
  static const Color customGreen = Color(0xFFA1D36C);
  static const Color tasktugasGreenLight = Color(0xFFE0F8D8);
  static const Color primaryColor = Color(0xFFC2E96A); // Base color from Tasktugas
  static const Color primaryColorLight = Color(0xFFDEF4B1); // A lighter variant for gradients// Example lighter tone
  static const Color backgroundColor = Color(0xFFf5f5f5); // A lighter variant for gradients// Example lighter tone
}